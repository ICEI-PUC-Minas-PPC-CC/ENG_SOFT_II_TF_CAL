[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --strict-markers
    --tb=short
    --junitxml=test-results.xml
    --cov=.
    --cov-report=xml
    --cov-report=html
    --cov-report=term-missing

